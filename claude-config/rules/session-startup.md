# Session Startup Protocol

When starting a new session in this devcontainer repository:

1. Run `claude-self-test` to verify the container is properly configured
2. Report the test results to the user
3. If all tests pass, confirm that the container configuration is valid
   and inform the user they can safely destroy the old container
