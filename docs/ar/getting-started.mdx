---
title: البدء
description: حمّل ملفاً واحداً، شغّل الحاوية، واتصل — لا حاجة لـ Git أو الاستنساخ.
sidebar:
  order: 2
i18n:
  sourceHash: 98032319278a
  translator: machine
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

:::tip[تثبيت سريع على macOS]
إذا كنت تستخدم macOS مع معالج Apple Silicon، تخطَّ هذه الصفحة واستخدم [المثبّت بأمر واحد](/devcontainer/install/):

```bash
curl -fsSL https://f5-sales-demo.github.io/devcontainer/scripts/install.sh | bash
```

يتولى التعامل مع حزم Homebrew وإعدادات iTerm2 وتهيئة Podman ويحمّل كل ما تحتاجه.
::::

## المتطلبات الأساسية

### بيئة تشغيل الحاويات

تحتاج إلى بيئة تشغيل حاويات على جهازك. يعمل كل من Docker أو Podman.

<Tabs syncKey="runtime">
<TabItem label="Docker">

| المنصة | التثبيت |
|--------|---------|
| macOS | [Docker Desktop لنظام Mac](https://docs.docker.com/desktop/install/mac-install/) |
| Windows | [Docker Desktop لنظام Windows](https://docs.docker.com/desktop/install/windows-install/) (واجهة WSL2 الخلفية) |
| Linux | [Docker Engine](https://docs.docker.com/engine/install/) أو [Docker Desktop لنظام Linux](https://docs.docker.com/desktop/install/linux/) |

تحقق من أنه يعمل:

```bash
docker --version
docker compose version
```

</TabItem>
<TabItem label="Podman">

| المنصة | التثبيت |
|--------|---------|
| macOS | [Podman Desktop لنظام Mac](https://podman-desktop.io/docs/installation/macos-install) |
| Windows | [Podman Desktop لنظام Windows](https://podman-desktop.io/docs/installation/windows-install) |
| Linux | [Podman](https://podman.io/docs/installation#installing-on-linux) + [Podman Compose](https://github.com/containers/podman-compose#installation) |

تحقق من أنه يعمل:

```bash
podman --version
podman-compose version
```

:::note[Linux فقط]
يشغّل Podman على Linux الحاويات مباشرة (بدون آلة افتراضية). تأكد من أن مقبس Podman نشط حتى يتمكن `podman-compose` من الاتصال:

```bash
systemctl --user enable --now podman.socket
```
:::

</TabItem>
</Tabs>

### موارد النظام

| المورد | الحد الأدنى | الموصى به |
|--------|-------------|-----------|
| القرص | 5 جيجابايت | 10 جيجابايت أو أكثر |
| الذاكرة | 8 جيجابايت مخصصة لبيئة تشغيل الحاويات | 12 جيجابايت أو أكثر |
| المعالج | نواتان | 4 أنوية أو أكثر |

<Tabs syncKey="runtime">
<TabItem label="Docker">

اضبط حدود الموارد في Docker Desktop &rarr; Settings &rarr; Resources.

</TabItem>
<TabItem label="Podman">

على **macOS/Windows**، اضبط حدود الموارد في Podman Desktop &rarr; Settings &rarr; Resources، أو عبر سطر الأوامر:

```bash
podman machine set --cpus 4 --memory 12288
podman machine stop && podman machine start
```

على **Linux**، تشارك الحاويات موارد المضيف مباشرة — لا حاجة لتهيئة الموارد.

</TabItem>
</Tabs>

## 1. الإعداد

أنشئ مجلداً وحمّل ملف compose:

```bash
mkdir devcontainer && cd devcontainer
curl -fsSLO https://raw.githubusercontent.com/f5-sales-demo/devcontainer/main/docker-compose.yml
```

## 2. إضافة بيانات المصادقة (اختياري)

أنشئ ملف `.env` في نفس المجلد لتهيئة Claude Code مسبقاً. اختر أحد الخيارات التالية:

### الخيار أ: Claude Max (OAuth)

```ini
CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-your-token-here
```

### الخيار ب: LiteLLM أو وكيل متوافق مع Anthropic

إذا كان لديك مثيل LiteLLM أو وكيل آخر متوافق مع Anthropic، عيّن هذين المتغيرين:

```ini
LITELLM_BASE_URL=https://your-litellm-endpoint.example.com
LITELLM_API_KEY=your-api-key
```

عيّن النطاق فقط — تستنتج الحاوية لواحق URL الخاصة بالمزود تلقائياً (مثل `/anthropic` لـ Claude Code).

### الملء التلقائي من بيانات الاعتماد المحلية

إذا كان `git` و `gh` مُهيأين على جهازك المضيف، يمكنك ملء معظم قيم `.env` تلقائياً. ابدأ بنسخ القالب:

```bash
cp .env.example .env
```

ثم شغّل الأوامر التي تتوافق مع إعداداتك:

```bash
# Git identity
echo "GIT_AUTHOR_EMAIL=$(git config user.email)" >> .env
echo "GIT_AUTHOR_NAME=\"$(git config user.name)\"" >> .env

# Timezone
echo "TZ=$(readlink /etc/localtime | sed 's|.*/zoneinfo/||')" >> .env

# GitHub CLI token (requires: gh auth login)
echo "GH_TOKEN=$(gh auth token)" >> .env

# SSH key (if you use SSH for git)
echo "SSH_PRIVATE_KEY=$(base64 < ~/.ssh/id_ed25519)" >> .env
```

ثم حرّر `.env` لإضافة رمز OAuth أو إعدادات وكيل LiteLLM (انظر الخيارات أعلاه).

:::tip[مساعدو الذكاء الاصطناعي]
يحتوي ملف `.env.example` على علامات `@auto-detect` في كل متغير يمكن ملؤه من الأدوات المحلية. يمكن لمساعدي الذكاء الاصطناعي مثل Claude Code البحث عن هذه العلامات وتشغيل الأوامر المرتبطة بها لبناء ملف `.env` كامل تلقائياً.
:::

## 3. التشغيل

<Tabs syncKey="runtime">
<TabItem label="Docker">

```bash
docker compose up -d
```

</TabItem>
<TabItem label="Podman">

```bash
podman-compose pull && podman-compose up -d
```

</TabItem>
</Tabs>

يسحب التشغيل الأول الصورة المبنية مسبقاً من ghcr.io (حوالي دقيقة واحدة حسب سرعة اتصالك). تستخدم عمليات التشغيل اللاحقة الصورة المخزنة مؤقتاً وتستغرق ثوانٍ.

## 4. الاتصال

<Tabs syncKey="runtime">
<TabItem label="Docker">

```bash
docker compose exec dev zsh
```

</TabItem>
<TabItem label="Podman">

```bash
podman-compose exec dev zsh
```

</TabItem>
</Tabs>

## 5. التحقق

```bash
# AI tools
claude --version
codex --version
pi --version

# Languages
node --version
python3 --version
go version

# Cloud CLIs
aws --version
az --version
terraform --version
```

اختبر اتصال الذكاء الاصطناعي:

```bash
claude -p "Say hello"
```

## 6. مشروعك الأول

```bash
cd /workspace
git clone https://github.com/your-org/your-project.git
cd your-project
claude
```

## 7. العرض عن بُعد (noVNC)

تتضمن الحاوية شاشة افتراضية لمشاهدة وكلاء الذكاء الاصطناعي وهم يتحكمون في المتصفح. افتح `http://localhost:6080/vnc.html` للاتصال. انظر [العرض عن بُعد (noVNC)](./vnc) للتفاصيل.

## 8. أتمتة المتصفح (Chrome DevTools MCP)

يمكن لـ Claude Code التنقل في صفحات الويب والتقاط لقطات شاشة وفحص DOM باستخدام خادم Chrome DevTools MCP المدمج. لا حاجة لأي إعداد — يعمل تلقائياً في الوضع بدون واجهة. انظر [Chrome DevTools MCP](./chrome) للتفاصيل.

## 9. الإيقاف وإعادة التشغيل

<Tabs syncKey="runtime">
<TabItem label="Docker">

```bash
# إيقاف (يحافظ على البيانات)
docker compose down

# التشغيل مرة أخرى
docker compose up -d

# حذف كل شيء والبدء من جديد
docker compose down -v
docker compose up -d
```

</TabItem>
<TabItem label="Podman">

```bash
# إيقاف (يحافظ على البيانات)
podman-compose down

# التشغيل مرة أخرى (السحب يضمن أحدث صورة)
podman-compose pull && podman-compose up -d

# حذف كل شيء والبدء من جديد
podman-compose down -v
podman-compose pull && podman-compose up -d
```

</TabItem>
</Tabs>

تبقى الشيفرة البرمجية في `/workspace` والمجلد الرئيسي محفوظة عبر عمليات إعادة التشغيل. انظر [التهيئة — استمرارية البيانات](./configuration#data-persistence) للتفاصيل.

---

للبناء المحلي وتخصيص Dockerfile، انظر [التطوير المحلي](./local-development). لدمج VS Code، انظر [VS Code](./vscode). لخيارات العرض عن بُعد، انظر [العرض عن بُعد (noVNC)](./vnc).
