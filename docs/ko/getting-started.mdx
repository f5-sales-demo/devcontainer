---
title: 시작하기
description: '파일 하나를 다운로드하고, 컨테이너를 시작하고, 연결하세요 — Git이나 클론 작업이 필요 없습니다.'
sidebar:
  order: 2
i18n:
  sourceHash: 98032319278a
  translator: machine
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

:::tip[macOS 빠른 설치]
Apple Silicon이 탑재된 macOS를 사용하신다면, 이 페이지를 건너뛰고 [한 줄 설치 스크립트](/devcontainer/install/)를 사용하세요:

```bash
curl -fsSL https://f5-sales-demo.github.io/devcontainer/scripts/install.sh | bash
```

Homebrew 패키지, iTerm2 설정, Podman 설정 및 필요한 모든 항목의 다운로드를 자동으로 처리합니다.
::::

## 사전 요구 사항

### 컨테이너 런타임

시스템에 컨테이너 런타임이 필요합니다. Docker 또는 Podman 모두 사용 가능합니다.

<Tabs syncKey="runtime">
<TabItem label="Docker">

| 플랫폼 | 설치 |
|----------|---------|
| macOS | [Docker Desktop for Mac](https://docs.docker.com/desktop/install/mac-install/) |
| Windows | [Docker Desktop for Windows](https://docs.docker.com/desktop/install/windows-install/) (WSL2 백엔드) |
| Linux | [Docker Engine](https://docs.docker.com/engine/install/) 또는 [Docker Desktop for Linux](https://docs.docker.com/desktop/install/linux/) |

정상 작동 확인:

```bash
docker --version
docker compose version
```

</TabItem>
<TabItem label="Podman">

| 플랫폼 | 설치 |
|----------|---------|
| macOS | [Podman Desktop for Mac](https://podman-desktop.io/docs/installation/macos-install) |
| Windows | [Podman Desktop for Windows](https://podman-desktop.io/docs/installation/windows-install) |
| Linux | [Podman](https://podman.io/docs/installation#installing-on-linux) + [Podman Compose](https://github.com/containers/podman-compose#installation) |

정상 작동 확인:

```bash
podman --version
podman-compose version
```

:::note[Linux 전용]
Linux에서 Podman은 컨테이너를 직접 실행합니다(VM 없음). `podman-compose`가 연결할 수 있도록 Podman 소켓이 활성화되어 있는지 확인하세요:

```bash
systemctl --user enable --now podman.socket
```
:::

</TabItem>
</Tabs>

### 시스템 리소스

| 리소스 | 최소 | 권장 |
|----------|---------|-------------|
| 디스크 | 5 GB | 10 GB+ |
| RAM | 컨테이너 런타임에 8 GB 할당 | 12 GB+ |
| CPU | 2 코어 | 4+ 코어 |

<Tabs syncKey="runtime">
<TabItem label="Docker">

Docker Desktop &rarr; Settings &rarr; Resources에서 리소스 제한을 조정하세요.

</TabItem>
<TabItem label="Podman">

**macOS/Windows**에서는 Podman Desktop &rarr; Settings &rarr; Resources에서 리소스 제한을 조정하거나, CLI를 통해 설정할 수 있습니다:

```bash
podman machine set --cpus 4 --memory 12288
podman machine stop && podman machine start
```

**Linux**에서는 컨테이너가 호스트 리소스를 직접 공유하므로 별도의 리소스 설정이 필요 없습니다.

</TabItem>
</Tabs>

## 1. 설정

폴더를 생성하고 compose 파일을 다운로드합니다:

```bash
mkdir devcontainer && cd devcontainer
curl -fsSLO https://raw.githubusercontent.com/f5-sales-demo/devcontainer/main/docker-compose.yml
```

## 2. 인증 자격 증명 추가 (선택 사항)

같은 폴더에 `.env` 파일을 생성하여 Claude Code를 사전 설정합니다. 다음 옵션 중 하나를 선택하세요:

### 옵션 A: Claude Max (OAuth)

```ini
CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-your-token-here
```

### 옵션 B: LiteLLM 또는 Anthropic 호환 프록시

LiteLLM 인스턴스나 기타 Anthropic 호환 프록시가 있는 경우, 다음 두 변수를 설정하세요:

```ini
LITELLM_BASE_URL=https://your-litellm-endpoint.example.com
LITELLM_API_KEY=your-api-key
```

도메인만 설정하세요 — 컨테이너가 프로바이더별 URL 접미사를 자동으로 생성합니다(예: Claude Code의 경우 `/anthropic`).

### 로컬 자격 증명에서 자동 채우기

호스트에 `git`과 `gh`가 설정되어 있다면, 대부분의 `.env` 값을 자동으로 채울 수 있습니다. 먼저 템플릿을 복사하세요:

```bash
cp .env.example .env
```

그런 다음 사용 환경에 맞는 명령어를 실행하세요:

```bash
# Git identity
echo "GIT_AUTHOR_EMAIL=$(git config user.email)" >> .env
echo "GIT_AUTHOR_NAME=\"$(git config user.name)\"" >> .env

# Timezone
echo "TZ=$(readlink /etc/localtime | sed 's|.*/zoneinfo/||')" >> .env

# GitHub CLI token (requires: gh auth login)
echo "GH_TOKEN=$(gh auth token)" >> .env

# SSH key (if you use SSH for git)
echo "SSH_PRIVATE_KEY=$(base64 < ~/.ssh/id_ed25519)" >> .env
```

그런 다음 `.env`를 편집하여 OAuth 토큰 또는 LiteLLM 프록시 설정을 추가하세요(위의 옵션 참조).

:::tip[AI 어시스턴트]
`.env.example` 파일에는 로컬 도구에서 값을 채울 수 있는 모든 변수에 `@auto-detect` 마커가 포함되어 있습니다. Claude Code와 같은 AI 어시스턴트가 이러한 마커를 스캔하고 관련 명령어를 실행하여 완전한 `.env`를 자동으로 구성할 수 있습니다.
:::

## 3. 시작

<Tabs syncKey="runtime">
<TabItem label="Docker">

```bash
docker compose up -d
```

</TabItem>
<TabItem label="Podman">

```bash
podman-compose pull && podman-compose up -d
```

</TabItem>
</Tabs>

처음 실행 시 ghcr.io에서 사전 빌드된 이미지를 가져옵니다(연결 속도에 따라 약 1분 소요). 이후 시작 시에는 캐시된 이미지를 사용하므로 몇 초면 됩니다.

## 4. 연결

<Tabs syncKey="runtime">
<TabItem label="Docker">

```bash
docker compose exec dev zsh
```

</TabItem>
<TabItem label="Podman">

```bash
podman-compose exec dev zsh
```

</TabItem>
</Tabs>

## 5. 확인

```bash
# AI tools
claude --version
codex --version
pi --version

# Languages
node --version
python3 --version
go version

# Cloud CLIs
aws --version
az --version
terraform --version
```

AI 연결 테스트:

```bash
claude -p "Say hello"
```

## 6. 첫 번째 프로젝트

```bash
cd /workspace
git clone https://github.com/your-org/your-project.git
cd your-project
claude
```

## 7. 원격 디스플레이 (noVNC)

컨테이너에는 AI 에이전트가 브라우저를 제어하는 것을 관찰할 수 있는 가상 디스플레이가 포함되어 있습니다. `http://localhost:6080/vnc.html`을 열어 연결하세요. 자세한 내용은 [원격 디스플레이 (noVNC)](./vnc)를 참조하세요.

## 8. 브라우저 자동화 (Chrome DevTools MCP)

Claude Code는 내장된 Chrome DevTools MCP 서버를 사용하여 웹 페이지를 탐색하고, 스크린샷을 찍고, DOM을 검사할 수 있습니다. 별도의 설정이 필요 없으며 — 헤드리스 모드에서 자동으로 작동합니다. 자세한 내용은 [Chrome DevTools MCP](./chrome)를 참조하세요.

## 9. 중지 및 재시작

<Tabs syncKey="runtime">
<TabItem label="Docker">

```bash
# 중지 (데이터 유지)
docker compose down

# 다시 시작
docker compose up -d

# 모든 것을 삭제하고 처음부터 시작
docker compose down -v
docker compose up -d
```

</TabItem>
<TabItem label="Podman">

```bash
# 중지 (데이터 유지)
podman-compose down

# 다시 시작 (pull로 최신 이미지 확인)
podman-compose pull && podman-compose up -d

# 모든 것을 삭제하고 처음부터 시작
podman-compose down -v
podman-compose pull && podman-compose up -d
```

</TabItem>
</Tabs>

`/workspace`의 코드와 홈 디렉토리는 재시작 후에도 유지됩니다. 자세한 내용은 [설정 — 데이터 지속성](./configuration#data-persistence)을 참조하세요.

---

로컬 빌드 및 Dockerfile 커스터마이징에 대해서는 [로컬 개발](./local-development)을 참조하세요. VS Code 통합에 대해서는 [VS Code](./vscode)를 참조하세요. 원격 디스플레이 옵션에 대해서는 [원격 디스플레이 (noVNC)](./vnc)를 참조하세요.
